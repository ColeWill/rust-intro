fn find_smallest_int(arr: &[i32]) -> i32 {
    // I will fill this in
 
}

fn main() {
   

    let count = count_sheep(testData);
}


#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn find_smallest_int() {
        assert_eq!(find_smallest_int(&[34, 15, 88, 2]), 2);
        assert_eq!(find_smallest_int(&[34, -345, -1, 100]), -345);
    }
}
